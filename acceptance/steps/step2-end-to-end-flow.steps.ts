// Step definitions from this file have been reorganised into domain-specific files:
// - background.steps.ts  (opens app, login, start transaction)
// - sale.steps.ts         (scan items, quantity, delete, cart verification, performance)
// - discount.steps.ts     (amount off, percent off, AC12 panel)
// - payment.steps.ts      (tender selection, payment confirmation, tender screen)
// - cancel.steps.ts       (cancel transaction, cart empty verification)
// - negative.steps.ts     (invalid login, unknown barcode)

